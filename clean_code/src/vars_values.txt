1) Инициализация переменных. Перенес объявлений переменных из начала функции к месту их первого использования.
Стало
// some code...
int fragmentShader = loadFragmentShader(...)
int vertexShader = loadVertexShader(...)
//using fragmentShader in GLES
//using vertexShader in GLES

2) Инициализация переменных. Перенес объявление переменных к функциям, в которых происходит обращение.
Стало
//...
Observer<PaymentMethod> paymentMethodObserver = Observer.create<PaymentMethod>();

void handlerPaymentMethods() {
    // update payment methods listener
}

void updatePaymentMethods() {
    paymentMethodObserver.onNext(...);
}

3) Добавил модификатор final переменной-флагу DEBUG.

4) Инварианты. Добавил в логи и метрику для ошибок информацию, если одна из метричных переменных отсылается с дефолтным значением (77),
что означает, что переменная на экране не была проинициализирована корректным значением.

5) Инварианты. Дополнил отладочной информацией инициализацию компонентов (такие, как эксперименты, фича-тоглы) на старте приложения.

6) Инварианты. Добавил отладочную информацию на вызов функций жизненного цикла экрана для упрощения отладки и понимания, в каком состоянии находится/находился экран.

7) Занулил объект UserDetail, после заполнения экрана значениями этого объекта.

8) Занулил объект EventDetail, после заполнения экрана значениями этого объекта.

9) Занулил объект PostDetail, после заполнения экрана значениями этого объекта.

10) Присвоил константу NO_ID=-1 переменным, ссылающимся на файлы-ресурсов экрана пользователя (такие, как иконки, строки) после того, вью-элементы были проинициализированы.

11) Переместил переменную userDetailsView, ссылающуюся на элемент экрана, к методу заполнения данных пользователя - initUserDetails.

12) Переместил переменную eventDetailsView, ссылающуюся на элемент экрана, к методу заполнения данных пользователя - initEventDetails.

13) Переместил переменную postDetailsView, ссылающуюся на элемент экрана, к методу заполнения данных пользователя - initPostDetails.

14) Добавил логирование в метрику ошибок, если обязательное поле пользователя не проинициализировано.

15) Также добавил в логи информацию, если опциональные поля пользователя = null.
