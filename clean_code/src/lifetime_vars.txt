// Старый проект
1) Глобальная переменная, эмулирующая in-memory cache для пользователя. Сделал приватной, добавил методы модификации переменной, сделал thread-safety.
2) Такой же рафакторинг для переменной EventDetail.
3) Такой же рафакторинг для переменной PostDetail.
4) Такой же рафакторинг для переменной NotificationParams.


5) Сократил "окно" возможно использования переменной srcRectangle, объявив перед применением. После этого занулил.

6) Сократил "окно" возможно использования переменной dstRectangle, объявив перед применением.
 Кроме того, оказалось, что она применяется только при определенном условии в if. Так, количество аллокаций сократилось.

7) Сократил "окно" возможно использования переменной canvas.

8) Сократил "окно" возможно использования переменной paint.

9) Сократил "окно" возможно использования переменной bitmap.

10) Вынес в локальную функцию переменную isSummaryLoaded, разрулил выполнение запросов через callback.

11) Вынес группу команд, связанных с уменьшением высоты списка на экране в зависимости от видимости кнопки "В корзину", в отдельную функцию.
Повысилась читабельность функции, отрисовывающей список, группа операций с условием выделена в отдельную логическую функцию.

12) Переместил переменную flipHalfReached с переменной класса в локальную переменную функции.

13) Переместил функционал галлереи изображений в отдельный модуль,
установил видимость только внутри модуля для всех вспомогательных переменных, функций. Публичным остается только точка входа в экран.

// Телеграмм
14) Получилось уменьшить область видимость с пакетной до приватной у переменной nextFrameTimeNs, так как вне класса не было обращений.

15) Также уменьшил видимость с пакетной до приватной у переменной firstFrameRendered.

